# Blueprint: Python Flask Microservice
Goal: generate a small production-ready Flask microservice:
- /health, /api/echo, /api/items CRUD in-memory + optional JSON persistence
- Dockerfile + requirements.txt
- README with env vars + run commands
Output MUST be [[FILE:...]] blocks only.
